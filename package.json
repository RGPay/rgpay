{
  "name": "rgpay",
  "version": "1.0.0",
  "description": "RGPay - Payment Management System",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "node index.js",
    "dev": "cd backend && npm run start:dev",
    "build:vercel": "cd frontend && npm ci && npm run build"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.1",
    "@nestjs/core": "^11.0.1",
    "@nestjs/platform-express": "^11.0.1",
    "express": "^5.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "dotenv": "^16.5.0",
    "jwt-decode": "^4.0.0",
    "mysql2": "^3.14.1",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@types/jwt-decode": "^2.2.1",
    "@types/multer": "^1.4.12",
    "sequelize-cli": "^6.6.3"
  }
}
